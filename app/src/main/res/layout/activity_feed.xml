<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FeedActivity">

  <!-- The Header (Unchanged, still at the top) -->
  <androidx.constraintlayout.widget.ConstraintLayout
      android:id="@+id/headerLayout"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:padding="16dp"
      app:layout_constraintTop_toTopOf="parent"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent">
    <!-- Your header content remains the same -->
    <ImageView
        android:id="@+id/ivProfilePicture"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@tools:sample/avatars" />
    <TextView
        android:id="@+id/tvUsername"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/ivProfilePicture"
        app:layout_constraintEnd_toStartOf="@id/buttonLogout"
        app:layout_constraintTop_toTopOf="@id/ivProfilePicture"
        app:layout_constraintBottom_toBottomOf="@id/ivProfilePicture"
        tools:text="Username" />
    <Button
        android:id="@+id/buttonLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Logout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/ivProfilePicture"
        app:layout_constraintBottom_toBottomOf="@id/ivProfilePicture"/>
  </androidx.constraintlayout.widget.ConstraintLayout>

  <!--
  CHANGE 1: A Guideline to create the 20% vertical space.
  This invisible line is positioned 20% down from the top of the screen.
  -->
  <androidx.constraintlayout.widget.Guideline
      android:id="@+id/space_guideline"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:orientation="horizontal"
      app:layout_constraintGuide_begin="168dp" />

  <!-- The Add Task Button (Now constrained to the guideline) -->
  <com.google.android.material.floatingactionbutton.FloatingActionButton
      android:id="@+id/fabAddTask"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginEnd="16dp"
      app:srcCompat="@drawable/ic_add"
      app:fabSize="auto"
      app:layout_constraintTop_toBottomOf="@id/space_guideline"
      app:layout_constraintEnd_toEndOf="parent"
      app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.SmallComponent" />

  <!-- The Settings Button (Still at the bottom right) -->
  <ImageButton
      android:id="@+id/buttonSettings"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:background="?attr/selectableItemBackgroundBorderless"
      android:contentDescription="Settings"
      android:padding="16dp"
      android:src="@drawable/ic_settings"
      app:tint="?android:attr/textColorSecondary"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintEnd_toEndOf="parent" />

  <!--
  CHANGE 2: The Task List (RecyclerView) is now constrained differently.
  - Its top is below the Add Task button.
  - Its bottom is constrained to the TOP of the Settings button.
  This prevents them from ever overlapping.
  -->
  <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/tasksRecyclerView"
      android:layout_width="match_parent"
      android:layout_height="0dp"
      android:layout_marginTop="8dp"
      app:layout_constraintTop_toBottomOf="@id/fabAddTask"
      app:layout_constraintBottom_toTopOf="@id/buttonSettings"
      tools:listitem="@layout/task_item" />

</androidx.constraintlayout.widget.ConstraintLayout>
